prefix_length: 500
strategy_horizon: 200
budgets: [0.09, 0.18, 0.30]
max_budget: 0.30

family_top_k: 3
selected_family_count_choices: [1, 2]
primitive_top_k: 3
selected_primitive_count_choices: [1, 2]
primitive_dirichlet_alpha: 0.7
primitive_noise_scale: 0.05

completion_set_size: 4
completion_mean_weight: 0.5
completion_min_weight: 0.5

num_candidates: 48
target_count: 8
quality_target_count: 4
diverse_target_count: 4

score_entropy_weight: 0.30
score_pseudo_weight: 0.30
score_margin_weight: 0.20
score_max_weight: 0.20
robust_min_weight: 0.25
attacker_gap_weight: 0.25
min_df_gain: 0.0
min_rf_gain: 0.0

max_dummy_per_slot: 8
max_consecutive_dummy_run: 24
max_tail_extension_ratio: 0.10
max_local_dummy_density: 0.80
density_penalty_weight: 0.10
tail_penalty_weight: 0.10
direction_penalty_weight: 0.05
burst_penalty_weight: 0.05

allocation_l1_weight: 0.5
allocation_cosine_weight: 0.5
clr_epsilon: 1.0e-6

diffusion_steps: 100
sampling_steps: 20
beta_schedule: linear
hidden_dim: 384
num_layers: 3
batch_size: auto
learning_rate: 0.0002
weight_decay: 0.00001
epochs: 10
gradient_clip: 1.0
use_amp: true

lambda_eps: 1.0
lambda_x0: 0.10
lambda_alloc: 0.10
lambda_effect: 0.03
lambda_family: 0.05
lambda_primitive: 0.05
lambda_struct: 0.03
lambda_fusion: 0.01
lambda_smooth: 0.01

target_sampling_temperature: 1.0
target_uniform_ratio: 0.40

seed: 0
num_workers: 0
cache_size: 1024
