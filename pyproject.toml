[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dynapd"
version = "0.1.0"
description = "Stage B dynamic padding defense research code for website fingerprinting experiments."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "torch",
  "tqdm",
]

[tool.setuptools.packages.find]
include = ["dynapd*"]
